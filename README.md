# start-git
pwd - показать рабочую папку
ls - содержимое папки
cd - перемещение / cd .. - переход на уровень вверх
mkdir - создание папки
touch - создание файла
mv - переименовать файл
rm - удаление файла
rmdir - удаление папки / rm -r - принудительное удаление папки
echo 
echo "Hello, practicum" >> practicum.txt - добавление в файл
cat - выведит содержимое файла

__________________________________________________________

команды перемещения и просмотра директорий: cd, pwd, ls;
управления файлами и папками: mkdir, touch, rm;
чтения и записи данных: cat, echo.

__________________________________________________________

git --version / чтобы узнать акутальную версию на комьютере
xcode-select --install / если команда выше, не найдена

Добавление логина и почты
git config --global user.name "Practicum"
git config --global user.email practicum@ya.ru

git config --list / вывод всех свойств конфига

___________________________________________________________

git init / создание репозитория

«Разгитить» папку, если что-то пошло не так, — rm -rf .git

Если вы случайно сделали Git-репозиторием не ту папку, её можно «разгитить». Для этого нужно удалить скрытую подпапку .git.

cd <папка с репозиторием> # перешли в папку
rm -rf .git # удалили подпапку .git 
Разберём подробнее, что такое -rf:
ключ -r (от англ. recursive — «рекурсивно») позволяет удалять папки вместе с их содержимым
ключ -f (от англ. force — «заставить») избавит вас от вопросов вроде «Вы точно хотите удалить этот файл? А этот? И этот тоже?».

git status / состояние репозитория

git add / добавление файла 
git add - all / git add . 

___________________________________________________________

git commit -m "" / создание коммита

__________________________________________________________

Генерация SSH-ключа

ssh-keygen -t ed25519 -C "<ВАШ EMAIL при регистрации в GitHub>"

ssh-add --apple-use-keychain ~/.ssh/id_ed25519

pbcopy < ~/.ssh/id_ed25519.pub

_________________________________________________________

git branch — просматриваем ветки проекта

echo "Изучаю работу веток" >> README.md 
git add . && git commit -m "Обновить README" 

git checkout <название_ветки>: переключаемся между ветками

git checkout new_branch # перешли в новую ветку
  Switched to branch 'new_branch'

git branch # проверили

  * new_branch # теперь находимся тут
    main

___________________________________________________________
Сливаем ветки

git checkout main # переключились на главную ветку

git merge new_branch # объединили ветки
Updating 079cfbf..f30d441
Fast-forward
 README.md | 2 ++
 1 file changed, 2 insertions(+)
